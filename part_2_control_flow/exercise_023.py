"""
### 23. 打印九九乘法表

- **描述:** 在控制台打印出完整的九九乘法表。
- **提示:** 使用嵌套的 `for` 循环。外层循环控制行，内层循环控制列。
- **期待:** 输出一个格式整齐的乘法表，例如:
  ```
  1*1=1
  1*2=2 2*2=4
  ...
  1*9=9 2*9=18 ... 9*9=81
  ```
"""

def generate_multiplication_table():
    """
    生成九九乘法表的字符串。
    为了方便测试，我们将打印到控制台的操作改为返回一个大的字符串。
    :return: 包含完整乘法表的字符串。
    """
    # 在这里写下你的代码
    # 使用嵌套循环构建字符串
    # 注意处理格式，比如使用 `\t` (制表符) 和 `\n` (换行符)
    table_string = ""
    return table_string

if __name__ == '__main__':
    table = generate_multiplication_table()
    print(table)

# 注意:
# 打印到控制台的格式可能因环境而异 (比如制表符的宽度)。
# 在测试中，我们只检查核心内容和结构是否正确。 