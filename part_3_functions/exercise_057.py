"""
### 57. 文档字符串

- **描述:** 为你之前写的任意一个函数（例如计算阶乘的函数）添加一个文档字符串（docstring），然后使用 `help()` 函数或 `__doc__` 属性来查看它。
- **提示:** 文档字符串是函数体内的第一个语句，用三引号 `\"\"\"...\"\"\"` 括起来。
- **期待:** `help(factorial)` 或 `print(factorial.__doc__)` 能显示你写的描述信息。
"""

def factorial(n):
    # 在这里写下你的代码
    pass

# 注意：本练习旨在演示如何编写和访问文档字符串。
# 这是一项关于代码文档化的技能，而不是一个需要通过/失败测试的功能。
# 因此，我们不提供 test 文件，建议直接运行此脚本并观察输出。

if __name__ == '__main__':
    # 当你为上面的函数添加了文档字符串后，运行此脚本来查看它
    
    print("--- 使用 `__doc__` 属性查看文档字符串 ---")
    print(factorial.__doc__)
    
    print("\n" + "="*40 + "\n")
    
    print("--- 使用 `help()` 函数查看文档字符串 ---")
    # help() 会提供更完整的视图，并会暂停程序执行，直到用户退出帮助视图（按 'q'）
    help(factorial) 